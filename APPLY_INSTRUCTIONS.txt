1DM — Outlet name & map location updates
==========================================

What changed
------------
Four outlets in the Locations list (src/lib/site-data.ts) renamed and/or
repointed to real Google Maps links, per your instructions:

1. "1DM Shankar Nagar II" -> "1DM Civil Line"
   Still Coming Soon, map link still a generic search placeholder
   (no real pin given yet) — just retitled to Civil Line.

2. "1DM Shankar Nagar" -> "Slice By 1DM Shankar Nagar"
   Now points at the real Google Maps place link you sent
   (SLICE BY 1DM). Left as "Coming soon" since you didn't say it's
   open yet — just say the word once it's live and I'll flip it to a
   real menu-live listing.

3. "1DM Shailendra Nagar" -> "Slice By 1DM VIP Road"
   Still Coming Soon, map link still a generic search placeholder
   (you said this one's location is Coming Soon too).

4. "1DM VIP Road" -> "1DM Dumartarai"
   Now points at the real Google Maps place link you sent (1DM
   Specialty Coffee). Kept as a live, menu-available outlet with its
   existing hours, since you didn't mention those changing — flag it
   if the hours at the new location are actually different.

All four keep their spot in the Raipur section of /locations, the
homepage locations teaser, and the /menu location switcher — those all
read from this one file, so nothing else needed to change.

One thing I found while in here, unrelated to what you asked for
----------------------------------------------------------------
"1DM VIP Road" (now "1DM Dumartarai") has been listed as a "live,
menu-available" outlet, but there's actually no menu content wired up
for it in site-data.ts — only Samta and Bilaspur have real menu data.
Visiting /menu/dumartarai (previously /menu/vip-road, same gap) shows
a blank/not-found page instead of a menu. This isn't something I
introduced — it was already true before this change, just hiding
under the old name. Send over the Dumartarai menu whenever you have
it and I'll wire it in.

What's in this patch
---------------------
- src/lib/site-data.ts — the only file touched.

Apply it
--------
1. cd ~/Downloads
   rm -rf 1dm-outlet-updates
   ZIP=$(ls -t 1dm-outlet-updates*.zip | head -1)
   echo "Using: $ZIP"
   unzip -o "$ZIP" -d 1dm-outlet-updates

2. cd /home/coffee/Downloads/1dm-website
   cp -r ~/Downloads/1dm-outlet-updates/. /home/coffee/Downloads/1dm-website/

3. rm -rf .next
   npm run build -- --webpack
   npm run start

4. Check http://localhost:3000/locations — you should see 1DM Civil
   Line, Slice By 1DM Shankar Nagar, Slice By 1DM VIP Road, and 1DM
   Dumartarai in place of the four old names, with the map pin icon
   on Shankar Nagar and Dumartarai opening your real Google Maps
   listings.

5. git add -A
   git commit -m "Update outlet names and map links for 4 locations"
   git push

Worth knowing
-------------
- Verified: tsc --noEmit, eslint, and a clean production build
  (next build -- --webpack) all pass with no errors — 16/16 pages
  generated, including the renamed /menu/dumartarai route.
- Nothing else in the codebase referenced the old names/slugs by
  string, so this one file is a complete, self-contained patch.
